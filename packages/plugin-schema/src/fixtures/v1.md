---
name: Docker Buildx
icon: https://example.org/icon.svg
description: Build multiarch container images
author: woodpecker-plugins
tags:
  - docker
  - build
containerImage: woodpeckerci/plugin-docker-buildx
containerImageUrl: https://hub.docker.com/r/woodpeckerci/plugin-docker-buildx
url: https://codeberg.org/woodpecker-plugins/docker-buildx
---

# Docker Buildx

Woodpecker CI plugin to build multiarch Docker images with buildx.

## Settings

<!-- woodpecker-plugin-settings v1 -->

| Name         | Type                | Required | Default            | Description                   |
| ------------ | ------------------- | -------- | ------------------ | ----------------------------- |
| `dry_run`    | `bool`              | no       | `false`            | disables docker push          |
| `repo`       | `list<string>`      | yes      | _none_             | image repo name(s)            |
| `password`   | `secret`            | no       | _none_             | registry password / token     |
| `registry`   | `string`            | no       | `docker.io`        | registry to authenticate with |
| `tag`/`tags` | `string`            | no       | _none_             | image tags                    |
| `logins`     | `object`            | no       | _none_             | see example; nested map       |
| `platform`   | `enum(amd64,arm64)` | no       | `amd64`            | target platform               |
| `timeout`    | `duration`          | no       | `10m`              | build timeout                 |
| `retries`    | `int`               | no       | `1`                | push retry count              |
| `ref`        | `string`            | no       | `${CI_COMMIT_REF}` | git ref to label the image    |

## Example

```yaml
steps:
  build:
    image: woodpeckerci/plugin-docker-buildx
```
